<% p = CommentPresenter.new(@comment, self) %>
<table class="table table-striped">
  <% p = CommentPresenter.new(@comment, self) %>
  <%= link_to image_tag(@comment.creator.icon_image, alt: 'icon', class: 'icon-tiny'), @comment.creator %>
  <%= p.creator_link %>
  <%= p.created_at %>
</table>
<div><%= formatted_content(@comment.content) %></div>
