<%= form_for SearchForm.new(params[:search]), as: 'search', url: :root,
    html: { method: :get, class: 'search' } do |f| %>
  <%= f.search_field :title  %>
  <%= f.submit('検索', class: 'btn btn-default btn-xs') %>
<% end %>
