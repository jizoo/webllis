<% provide(:title, '投稿一覧') %>
<h1>投稿一覧</h1>

<div class="row">
  <div class="col-md-12">
  <h3>投稿 <span class="badge"><%= current_user.posts.count %></span></h3>
    <ul class="media-list">
      <%= render @posts %>
    </ul>
    <%= paginate @posts %>
  </div>
</div>
