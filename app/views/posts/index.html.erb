<% provide(:title, '自分投稿一覧') %>
<h1>自分の投稿一覧</h1>

<div class="row">
  <div class="col-md-12">
  <h3>自分の投稿 <span class="badge"><%= current_user.posts.count %></span></h3>
    <ul class="media-list">
      <%= render @posts %>
    </ul>
    <%= paginate @posts %>
  </div>
</div>
